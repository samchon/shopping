"use client";import { useState } from "react";import Link from "next/link";import { Button } from "@/components/ui/button";import { formatPrice } from "@/utils/format";export default function ChargeDepositPage() {  const [amount, setAmount] = useState("");  const [method, setMethod] = useState("card");  const handleCharge = (e: React.FormEvent) => {    e.preventDefault();    alert("Deposit charge functionality - Backend integration needed");  };  return (    <div className="min-h-screen bg-gray-50">      <header className="bg-white shadow-sm">        <div className="max-w-7xl mx-auto px-4 py-4">          <Link href="/customer/wallet" className="text-blue-600 hover:underline">            &larr; Back to Wallet          </Link>        </div>      </header>      <main className="max-w-2xl mx-auto px-4 py-8">        <h1 className="text-3xl font-bold text-gray-900 mb-8">Charge Deposit</h1>        <div className="bg-white rounded-lg shadow-sm p-6">          <form onSubmit={handleCharge} className="space-y-6">            <div>              <label className="block text-sm font-medium mb-2">Amount (KRW)</label>              <input                type="number"                required                min="10000"                step="10000"                className="w-full px-4 py-2 border border-gray-300 rounded-lg"                value={amount}                onChange={(e) => setAmount(e.target.value)}                placeholder="Enter amount (minimum 10,000)"              />            </div>            <div>              <label className="block text-sm font-medium mb-2">Payment Method</label>              <div className="space-y-2">                <label className="flex items-center space-x-2">                  <input type="radio" name="method" checked={method === "card"} onChange={() => setMethod("card")} />                  <span>Credit/Debit Card</span>                </label>                <label className="flex items-center space-x-2">                  <input type="radio" name="method" checked={method === "bank"} onChange={() => setMethod("bank")} />                  <span>Bank Transfer</span>                </label>                <label className="flex items-center space-x-2">                  <input type="radio" name="method" checked={method === "kakao"} onChange={() => setMethod("kakao")} />                  <span>Kakao Pay</span>                </label>              </div>            </div>            <div className="p-4 bg-blue-50 rounded-lg">              <p className="text-sm text-blue-800">                <strong>Note:</strong> Deposits can be used for future purchases and will be refunded when you close your account.              </p>            </div>            <Button type="submit" className="w-full py-3">              Charge {amount ? formatPrice(parseInt(amount)) : "Deposit"}            </Button>          </form>        </div>      </main>    </div>  );}
